# Circuit

This page records the electrical integration references for the current Omniseer
robot hardware. The repository keeps source hardware material under
`hardware/electrical/` and firmware-facing IO configuration under `firmware/include/`.
